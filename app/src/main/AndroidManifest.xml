<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.example.workouttracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.ExerciseInput"></activity>
        <activity android:name=".Activities.DayEditCreate" />
        <activity android:name=".Activities.RoutineEditCreate" />
        <activity android:name=".History" />
        <activity android:name=".Stats" />
        <activity android:name=".Activities.SelectEdit" />
        <activity android:name=".Execute" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>